package com.array;

/**
 * Create a heap of arr[N] -Anytime insert new number, compare each with
 * min(arr[N]) and max[arr[N]) -Sort arr[N] -O(N.n)
 * 
 * @author ssachdev
 * 
 */
public class FindTopN_Integer {

}
